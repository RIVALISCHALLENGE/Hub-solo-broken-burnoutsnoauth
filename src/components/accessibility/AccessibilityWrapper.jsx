import React from "react";

// Lightweight placeholder wrapper for accessibility features.
// Intentionally minimal — preserves API expected by imports elsewhere.
export function AccessibilityWrapper({ children }) {
  return <>{children}</>;
}

export default AccessibilityWrapper;
